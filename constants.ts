export const CERTIFICATE_STACK = 'certificateStack' as const
export const DOMAIN_MAIN = 'kfmc.casa' as const
export const DOMAIN_SUB = 'www' as const
export const CERTIFICATE_ID = `certificate-${DOMAIN_SUB}-${DOMAIN_MAIN}` as const
export const ZONE_ID = `ZONE_ID` as const
export const ENV = {
  account: '411836754714',
  region: 'us-east-1',
} as const
